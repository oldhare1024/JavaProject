package swing;

import java.awt.*;

public class WindowDemo {
    public static void main(String[] args) {
        Frame frame = new Frame();//创建一个窗口对象
        frame.setLocation(100, 100);//指定窗口的位置，大小
        frame.setVisible(true);//设置窗口可见
        //创建一个window对象，因为容器都必须依附于window存在
        //创建一个panel对象
        //创建一个文本框和一个按钮，并且将他们放入到panel容器中
        //设置window的位置以及大小
        //设置window可见性
    }
}
